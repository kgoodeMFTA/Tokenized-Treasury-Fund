{
  "name": "tokenized-treasury-fund",
  "version": "1.0.0",
  "description": "Portfolio project: a permissioned ERC-20 tokenized money-market fund modeled on BUIDL / OUSG / BENJI mechanics, with BA artifacts for a Tokenization Business Analyst role.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "simulate": "hardhat run scripts/simulate-lifecycle.js --network hardhat"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
